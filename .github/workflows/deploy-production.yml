name: Deploy Production

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: read

env:
  AWS_DEFAULT_REGION: us-east-1
  AWS_REGION: us-east-1
  PYTHONDONTWRITEBYTECODE: 1
  SAM_CLI_TELEMETRY: 0

concurrency:
  group: data-spaces-production-${{ github.repository }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate:
    runs-on: ubuntu-latest
    outputs:
      artifact_id: ${{ steps.upload.outputs.artifact-id }}
      artifact_name: ${{ steps.metadata.outputs.artifact_name }}
      manifest_digest: ${{ steps.metadata.outputs.manifest_digest }}
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          ref: ${{ github.sha }}
          fetch-depth: 1
          persist-credentials: false

      - name: Verify exact clean production commit
        shell: bash
        env:
          EXPECTED_SHA: ${{ github.sha }}
          CURRENT_REF: ${{ github.ref }}
        run: |
          set -euo pipefail
          test "$CURRENT_REF" = "refs/heads/main"
          test "$(git rev-parse HEAD)" = "$EXPECTED_SHA"
          test -z "$(git status --porcelain)"

      - name: Verify exact test-to-main merged PR
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8
        env:
          EXPECTED_SHA: ${{ github.sha }}
          SOURCE_BRANCH: test
          TARGET_BRANCH: main
        with:
          github-token: ${{ github.token }}
          script: |
            const fail = code => { throw new Error(code); };
            const isSha = value => /^[a-f0-9]{40}$/.test(value ?? '');
            const repository = process.env.GITHUB_REPOSITORY;
            const sha = process.env.EXPECTED_SHA;
            const sourceBranch = process.env.SOURCE_BRANCH;
            const targetBranch = process.env.TARGET_BRANCH;
            if (!repository || !isSha(sha) || process.env.GITHUB_REF !== `refs/heads/${targetBranch}`) fail('promotion_context_invalid');
            const [owner, repo] = repository.split('/');
            const { data: commit } = await github.rest.git.getCommit({ owner, repo, commit_sha: sha });
            const parents = Array.isArray(commit.parents) ? commit.parents.map(parent => parent.sha) : [];
            if (parents.length !== 2 || parents.some(parent => !isSha(parent))) fail('promotion_merge_commit_required');
            let pullRequests = [];
            for (let attempt = 1; attempt <= 3; attempt += 1) {
              const response = await github.request('GET /repos/{owner}/{repo}/commits/{commit_sha}/pulls', {
                owner, repo, commit_sha: sha, per_page: 100,
                headers: { accept: 'application/vnd.github+json', 'X-GitHub-Api-Version': '2026-03-10' },
              });
              pullRequests = response.data;
              if (!Array.isArray(pullRequests) || pullRequests.length > 100) fail('promotion_api_invalid_response');
              if (pullRequests.length > 0 || attempt === 3) break;
              await new Promise(resolve => setTimeout(resolve, 1500));
            }
            const matches = pullRequests.filter(pr => (
              pr?.state === 'closed' && pr.merged_at && (pr.merge_commit_sha == null || pr.merge_commit_sha === sha)
              && pr.base?.ref === targetBranch && pr.base?.repo?.full_name === repository && isSha(pr.base?.sha)
              && pr.head?.ref === sourceBranch && pr.head?.repo?.full_name === repository && isSha(pr.head?.sha)
            ));
            if (matches.length !== 1) fail(matches.length ? 'promotion_pr_ambiguous' : 'promotion_pr_not_found');
            const pr = matches[0];
            if (parents[0] !== pr.base.sha) fail('promotion_first_parent_mismatch');
            if (parents[1] !== pr.head.sha) fail('promotion_second_parent_mismatch');
            if (context.eventName === 'push') {
              const event = context.payload;
              if (event.forced === true || event.created === true || event.deleted === true || event.after !== sha
                || !isSha(event.before) || /^0+$/.test(event.before) || parents[0] !== event.before) fail('promotion_push_not_allowed');
            } else if (context.eventName !== 'workflow_dispatch') fail('promotion_event_not_allowed');
            const { data: branch } = await github.rest.repos.getBranch({ owner, repo, branch: targetBranch });
            if (branch.commit.sha !== sha) fail('promotion_target_tip_mismatch');

      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.13'

      - name: Install pinned runtime and test tooling
        run: |
          python -m pip install --disable-pip-version-check -r requirements.txt PyYAML==6.0.2 pip-audit==2.10.1
          python -m pip_audit -r requirements.txt

      - uses: aws-actions/setup-sam@89ddb14d60e682855e3fea4be85b3c56485de310 # v3
        with:
          version: 1.163.0
          use-installer: true
          token: ${{ github.token }}

      - name: Test, validate, and build exact candidate
        shell: bash
        run: |
          set -euo pipefail
          python -m unittest discover -s tests -p "test_*.py"
          sam validate --lint
          sam build --no-cached
          test -z "$(find .aws-sam/build -type l -print -quit)"
          python tools/verify_sam_build.py
          : > "$RUNNER_TEMP/expected-build-paths.txt"
          for function in ProtectedReadFunction ProtectedActionFunction PublicReadFunction InternalSnapshotReadFunction; do
            git ls-files 'src/**' | while IFS= read -r path; do
              printf './%s/%s\n' "$function" "${path#src/}"
            done >> "$RUNNER_TEMP/expected-build-paths.txt"
          done
          printf './template.yaml\n' >> "$RUNNER_TEMP/expected-build-paths.txt"
          LC_ALL=C sort -u -o "$RUNNER_TEMP/expected-build-paths.txt" "$RUNNER_TEMP/expected-build-paths.txt"
          (cd .aws-sam/build && LC_ALL=C find . -type f -print | LC_ALL=C sort) > "$RUNNER_TEMP/actual-build-paths.txt"
          diff -u "$RUNNER_TEMP/expected-build-paths.txt" "$RUNNER_TEMP/actual-build-paths.txt"

      - name: Write validated build manifest
        id: metadata
        shell: bash
        run: |
          set -euo pipefail
          test -z "$(find .aws-sam/build -type l -print -quit)"
          (cd .aws-sam/build && LC_ALL=C find . -type f -print0 | LC_ALL=C sort -z | xargs -0 sha256sum) > .aws-sam/build-manifest.sha256
          artifact_name="data-spaces-production-build-${{ github.run_id }}-${{ github.run_attempt }}-${{ github.sha }}"
          manifest_digest="$(sha256sum .aws-sam/build-manifest.sha256 | cut -d ' ' -f1)"
          [[ "$artifact_name" =~ ^data-spaces-production-build-[1-9][0-9]*-[1-9][0-9]*-[a-f0-9]{40}$ ]]
          [[ "$manifest_digest" =~ ^[a-f0-9]{64}$ ]]
          printf 'artifact_name=%s\nmanifest_digest=%s\n' "$artifact_name" "$manifest_digest" >> "$GITHUB_OUTPUT"

      - name: Upload exact validated artifact
        id: upload
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{ steps.metadata.outputs.artifact_name }}
          path: |
            .aws-sam/build/template.yaml
            .aws-sam/build/ProtectedReadFunction
            .aws-sam/build/ProtectedActionFunction
            .aws-sam/build/PublicReadFunction
            .aws-sam/build/InternalSnapshotReadFunction
            .aws-sam/build-manifest.sha256
          if-no-files-found: error
          include-hidden-files: true
          retention-days: 1

  deploy:
    runs-on: ubuntu-latest
    needs: validate
    environment: production
    permissions:
      contents: read
      id-token: write
      pull-requests: read
    env:
      AWS_ROLE_ARN: ${{ vars.AWS_ROLE_ARN }}
      AWS_CLOUDFORMATION_ROLE_ARN: ${{ vars.AWS_CLOUDFORMATION_ROLE_ARN }}
      CONFIG_REGISTRY_TABLE_PARAMETER: /zoolanding/production/config/registry-table-name
      CONFIG_PAYLOADS_BUCKET_PARAMETER: /zoolanding/production/config/payload-bucket-name
      AUTH_SESSION_TABLE_PARAMETER: /zoolanding/production/auth/session-table-name
      AUTH_USER_STATE_TABLE_PARAMETER: /zoolanding/production/auth/user-state-table-name
      INTERNAL_SNAPSHOT_CALLER_ROLE_ARN: ${{ vars.INTERNAL_SNAPSHOT_CALLER_ROLE_ARN }}
      ALARM_TOPIC_ARN: ${{ vars.ALARM_TOPIC_ARN }}
    steps:
      - name: Validate artifact metadata
        id: artifact
        shell: bash
        env:
          RAW_ARTIFACT_ID: ${{ needs.validate.outputs.artifact_id }}
          RAW_ARTIFACT_NAME: ${{ needs.validate.outputs.artifact_name }}
          RAW_MANIFEST_DIGEST: ${{ needs.validate.outputs.manifest_digest }}
          EXPECTED_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          [[ "$EXPECTED_SHA" =~ ^[a-f0-9]{40}$ ]]
          [[ "$RAW_ARTIFACT_ID" =~ ^[1-9][0-9]*$ ]]
          [[ "$RAW_ARTIFACT_ID" != *","* ]]
          [[ "$RAW_ARTIFACT_NAME" =~ ^data-spaces-production-build-[1-9][0-9]*-[1-9][0-9]*-${EXPECTED_SHA}$ ]]
          [[ "$RAW_MANIFEST_DIGEST" =~ ^[a-f0-9]{64}$ ]]
          printf 'id=%s\nname=%s\ndigest=%s\n' "$RAW_ARTIFACT_ID" "$RAW_ARTIFACT_NAME" "$RAW_MANIFEST_DIGEST" >> "$GITHUB_OUTPUT"

      - name: Download exact artifact from this run
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          artifact-ids: ${{ steps.artifact.outputs.id }}
          path: .aws-sam
          merge-multiple: true

      - name: Verify artifact integrity and closed file set
        shell: bash
        env:
          EXPECTED_MANIFEST_DIGEST: ${{ steps.artifact.outputs.digest }}
        run: |
          set -euo pipefail
          test -z "$(find .aws-sam/build -type l -print -quit)"
          test -f .aws-sam/build/template.yaml
          test -f .aws-sam/build-manifest.sha256
          test "$(sha256sum .aws-sam/build-manifest.sha256 | cut -d ' ' -f1)" = "$EXPECTED_MANIFEST_DIGEST"
          (cd .aws-sam/build && sha256sum --check --strict ../build-manifest.sha256)
          cut -c67- .aws-sam/build-manifest.sha256 | LC_ALL=C sort > "$RUNNER_TEMP/manifest-paths.txt"
          (cd .aws-sam/build && LC_ALL=C find . -type f -print | LC_ALL=C sort) > "$RUNNER_TEMP/artifact-paths.txt"
          diff -u "$RUNNER_TEMP/manifest-paths.txt" "$RUNNER_TEMP/artifact-paths.txt"

      - name: Reverify exact test-to-main merged PR before AWS credentials
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8
        env:
          EXPECTED_SHA: ${{ github.sha }}
          SOURCE_BRANCH: test
          TARGET_BRANCH: main
        with:
          github-token: ${{ github.token }}
          script: |
            const fail = code => { throw new Error(code); };
            const isSha = value => /^[a-f0-9]{40}$/.test(value ?? '');
            const repository = process.env.GITHUB_REPOSITORY;
            const sha = process.env.EXPECTED_SHA;
            const sourceBranch = process.env.SOURCE_BRANCH;
            const targetBranch = process.env.TARGET_BRANCH;
            if (!repository || !isSha(sha) || process.env.GITHUB_REF !== `refs/heads/${targetBranch}`) fail('promotion_context_invalid');
            const [owner, repo] = repository.split('/');
            const { data: commit } = await github.rest.git.getCommit({ owner, repo, commit_sha: sha });
            const parents = Array.isArray(commit.parents) ? commit.parents.map(parent => parent.sha) : [];
            if (parents.length !== 2 || parents.some(parent => !isSha(parent))) fail('promotion_merge_commit_required');
            let pullRequests = [];
            for (let attempt = 1; attempt <= 3; attempt += 1) {
              const response = await github.request('GET /repos/{owner}/{repo}/commits/{commit_sha}/pulls', {
                owner, repo, commit_sha: sha, per_page: 100,
                headers: { accept: 'application/vnd.github+json', 'X-GitHub-Api-Version': '2026-03-10' },
              });
              pullRequests = response.data;
              if (!Array.isArray(pullRequests) || pullRequests.length > 100) fail('promotion_api_invalid_response');
              if (pullRequests.length > 0 || attempt === 3) break;
              await new Promise(resolve => setTimeout(resolve, 1500));
            }
            const matches = pullRequests.filter(pr => (
              pr?.state === 'closed' && pr.merged_at && (pr.merge_commit_sha == null || pr.merge_commit_sha === sha)
              && pr.base?.ref === targetBranch && pr.base?.repo?.full_name === repository && isSha(pr.base?.sha)
              && pr.head?.ref === sourceBranch && pr.head?.repo?.full_name === repository && isSha(pr.head?.sha)
            ));
            if (matches.length !== 1) fail(matches.length ? 'promotion_pr_ambiguous' : 'promotion_pr_not_found');
            const pr = matches[0];
            if (parents[0] !== pr.base.sha) fail('promotion_first_parent_mismatch');
            if (parents[1] !== pr.head.sha) fail('promotion_second_parent_mismatch');
            if (context.eventName === 'push') {
              const event = context.payload;
              if (event.forced === true || event.created === true || event.deleted === true || event.after !== sha
                || !isSha(event.before) || /^0+$/.test(event.before) || parents[0] !== event.before) fail('promotion_push_not_allowed');
            } else if (context.eventName !== 'workflow_dispatch') fail('promotion_event_not_allowed');
            const { data: branch } = await github.rest.repos.getBranch({ owner, repo, branch: targetBranch });
            if (branch.commit.sha !== sha) fail('promotion_target_tip_mismatch');

      - uses: aws-actions/setup-sam@89ddb14d60e682855e3fea4be85b3c56485de310 # v3
        with:
          version: 1.163.0
          use-installer: true
          token: ${{ github.token }}

      - name: Validate deployment configuration
        shell: bash
        run: |
          set -euo pipefail
          role_pattern='^arn:(aws|aws-us-gov|aws-cn):iam::([0-9]{12}):role/[A-Za-z0-9+=,.@_/-]+$'
          topic_pattern='^arn:(aws|aws-us-gov|aws-cn):sns:([a-z0-9-]+):([0-9]{12}):[A-Za-z0-9_.-]+$'
          [[ "$AWS_ROLE_ARN" =~ $role_pattern ]]
          deployment_partition="${BASH_REMATCH[1]}"
          deployment_account="${BASH_REMATCH[2]}"
          [[ "$AWS_CLOUDFORMATION_ROLE_ARN" =~ $role_pattern ]]
          cloudformation_partition="${BASH_REMATCH[1]}"
          cloudformation_account="${BASH_REMATCH[2]}"
          [[ "$cloudformation_partition" = "$deployment_partition" ]]
          [[ "$cloudformation_account" = "$deployment_account" ]]
          test "$CONFIG_REGISTRY_TABLE_PARAMETER" = "/zoolanding/production/config/registry-table-name"
          test "$CONFIG_PAYLOADS_BUCKET_PARAMETER" = "/zoolanding/production/config/payload-bucket-name"
          test "$AUTH_SESSION_TABLE_PARAMETER" = "/zoolanding/production/auth/session-table-name"
          test "$AUTH_USER_STATE_TABLE_PARAMETER" = "/zoolanding/production/auth/user-state-table-name"
          if [[ -n "$INTERNAL_SNAPSHOT_CALLER_ROLE_ARN" ]]; then
            [[ "$INTERNAL_SNAPSHOT_CALLER_ROLE_ARN" =~ $role_pattern ]]
            caller_partition="${BASH_REMATCH[1]}"
            caller_account="${BASH_REMATCH[2]}"
            [[ "$caller_partition" = "$deployment_partition" ]]
            [[ "$caller_account" = "$deployment_account" ]]
          fi
          [[ "$ALARM_TOPIC_ARN" =~ $topic_pattern ]]
          alarm_partition="${BASH_REMATCH[1]}"
          alarm_region="${BASH_REMATCH[2]}"
          alarm_account="${BASH_REMATCH[3]}"
          [[ "$alarm_partition" = "$deployment_partition" ]]
          [[ "$alarm_account" = "$deployment_account" ]]
          [[ "$alarm_region" = "$AWS_REGION" ]]

      - uses: aws-actions/configure-aws-credentials@517a711dbcd0e402f90c77e7e2f81e849156e31d # v6
        with:
          role-to-assume: ${{ env.AWS_ROLE_ARN }}
          aws-region: us-east-1

      - name: Validate exact cross-service SSM values
        shell: bash
        run: |
          set -euo pipefail
          read_parameter() {
            local name="$1"
            local result type value extra
            result="$(aws ssm get-parameter --name "$name" --no-with-decryption --query 'Parameter.[Type,Value]' --output text)"
            IFS=$'\t' read -r type value extra <<< "$result"
            test "$type" = "String"
            test -n "$value"
            test -z "$extra"
            printf '%s' "$value"
          }
          config_registry_table="$(read_parameter "$CONFIG_REGISTRY_TABLE_PARAMETER")"
          config_payloads_bucket="$(read_parameter "$CONFIG_PAYLOADS_BUCKET_PARAMETER")"
          auth_session_table="$(read_parameter "$AUTH_SESSION_TABLE_PARAMETER")"
          auth_user_state_table="$(read_parameter "$AUTH_USER_STATE_TABLE_PARAMETER")"
          [[ "$config_registry_table" =~ ^[A-Za-z0-9_.-]{3,255}$ ]]
          [[ "$config_payloads_bucket" =~ ^[a-z0-9][a-z0-9.-]{1,61}[a-z0-9]$ ]]
          [[ "$auth_session_table" =~ ^[A-Za-z0-9_.-]{3,255}$ ]]
          [[ "$auth_user_state_table" =~ ^[A-Za-z0-9_.-]{3,255}$ ]]

      - name: Deploy production
        shell: bash
        run: |
          set -euo pipefail
          parameter_overrides=(
            "EnvironmentName=production"
            "ConfigRegistryTableName=/zoolanding/production/config/registry-table-name"
            "ConfigPayloadsBucketName=/zoolanding/production/config/payload-bucket-name"
            "AuthSessionTableName=/zoolanding/production/auth/session-table-name"
            "AuthUserStateTableName=/zoolanding/production/auth/user-state-table-name"
            "AlarmTopicArn=$ALARM_TOPIC_ARN"
          )
          if [[ -n "$INTERNAL_SNAPSHOT_CALLER_ROLE_ARN" ]]; then
            parameter_overrides+=("InternalSnapshotCallerRoleArn=$INTERNAL_SNAPSHOT_CALLER_ROLE_ARN")
          fi
          sam deploy \
            --template-file .aws-sam/build/template.yaml \
            --stack-name zoolanding-data-spaces-production \
            --region us-east-1 \
            --resolve-s3 \
            --s3-prefix zoolanding-data-spaces-production \
            --capabilities CAPABILITY_IAM \
            --role-arn "$AWS_CLOUDFORMATION_ROLE_ARN" \
            --no-confirm-changeset \
            --no-fail-on-empty-changeset \
            --parameter-overrides "${parameter_overrides[@]}"
